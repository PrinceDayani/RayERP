version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm ci --omit=dev
    build:
      - npm run build:apprunner
run:
  command: node dist/server.js
  network:
    port: 5000
  env:
    - name: NODE_ENV
      value: "production"
