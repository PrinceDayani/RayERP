version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install
    build:
      - npx tsc -p tsconfig.build.json
run:
  command: node dist/server.js
  network:
    port: 5000
  env:
    - name: NODE_ENV
      value: "production"
