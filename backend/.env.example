# Database
MONGO_URI=mongodb://localhost:27017/rayerp
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=your-secure-secret-key-min-32-chars
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_SECRET=your-refresh-token-secret-min-32-chars
REFRESH_TOKEN_EXPIRES_IN=7d

# Two-factor authentication
# 32-byte key (hex or base64) used to encrypt TOTP secrets at rest.
# Optional: only required before any user enrols in two-factor authentication.
# Generate with: openssl rand -hex 32
TWO_FACTOR_ENCRYPTION_KEY=

# CORS
CORS_ORIGIN=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Logging
LOG_LEVEL=info

# Redis (optional — caching is gracefully disabled when not available)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://127.0.0.1:6379

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# Company Details
COMPANY_NAME=RayERP
COMPANY_ADDRESS=Your Company Address
COMPANY_PHONE=+91-1234567890
COMPANY_EMAIL=info@rayerp.com

# Backup Configuration
BACKUP_DIR=./backups

# File Upload
UPLOAD_DIR=./public/uploads

# Seed roles, permissions, chart of accounts and currencies on startup.
# Leave unset/false so a cleared database is not repopulated on boot.
SEED_ON_STARTUP=false
